# ------------------------------------------------------------------------
# Modules
# ------------------------------------------------------------------------

import math
from math import sqrt, pi

print(math.floor(3.7))
print(math.ceil(3.2))
print(math.pow(2, 3))
print(sqrt(16))
print(pi)

import datetime as dt

print(dt.date.today())
